## Summary

<!-- 1–3 sentences: what does this PR change, and why? -->

## Type of change

<!-- Check the box(es) that apply -->

- [ ] Bug fix (non-breaking change which fixes an issue)
- [ ] New feature (non-breaking change which adds functionality)
- [ ] Refactor / cleanup (no behavior change)
- [ ] Performance improvement
- [ ] Documentation / config / CI
- [ ] Breaking change (would cause existing functionality to not work as expected)

## Test plan

<!-- How did you verify this works? Be specific.
  Examples:
    - "Signed in via Google on localhost, then again in incognito on the deploy preview"
    - "Ran `npm test -- intelligentScheduler` locally — passes"
    - "Triggered an error in DevTools, confirmed it appeared in Sentry within 30s"
-->

- [ ] …

## Risk

<!-- What could break? What's the blast radius if this PR is wrong?
  Examples:
    - "Touches Firebase rules — locked-out users if rule is wrong"
    - "Changes a hot path in geminiService — could break all AI features"
    - "Localized to one component, low risk"
-->

## Checks

- [ ] CI passes (`Build` + `Tests` jobs)
- [ ] Tested on the Netlify deploy preview (link in the bot comment)
- [ ] Reviewed Seer's code-review comments
- [ ] No new ESLint warnings in the build output
- [ ] No secrets / API keys committed

## Screenshots / video

<!-- For UI changes — drag-and-drop a screenshot or screen recording. -->
