[build]
  base = "ap-prep-hub"
  command = "npm run build"
  publish = "build"

[functions]
  directory = "netlify/functions"

# Without this, `netlify dev` reports "No app server detected" and serves the
# stale contents of ap-prep-hub/build as a static site — so source edits appear
# to do nothing and chunk hashes stop matching. Pin the CRA dev server instead.
[dev]
  command = "npm start"
  targetPort = 3000
  port = 8888
  publish = "build"
  autoLaunch = false

# --- Performance: Caching headers ---

# Hashed static assets (JS/CSS/media) — immutable, cache forever
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Fonts — long cache
[[headers]]
  for = "/*.woff2"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.woff"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Favicon and icons — moderate cache
[[headers]]
  for = "/*.ico"
  [headers.values]
    Cache-Control = "public, max-age=86400"

[[headers]]
  for = "/*.png"
  [headers.values]
    Cache-Control = "public, max-age=86400"

# HTML — revalidate every time (picks up new JS/CSS hashes)
[[headers]]
  for = "/"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Service worker — never cache
[[headers]]
  for = "/service-worker.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
